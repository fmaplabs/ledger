{
    "name": "ledger-cloud",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "convex dev --start 'pnpm --filter web dev'",
        "deploy:convex": "convex deploy",
        "deploy": "convex deploy --cmd 'pnpm --filter web run deploy' --cmd-url-env-var-name VITE_CONVEX_URL",
        "test": "vitest run"
    },
    "dependencies": {
        "@convex-dev/stripe": "^0.1.4",
        "@convex-dev/workos-authkit": "^0.2.7",
        "convex": "^1.31.0"
    },
    "devDependencies": {
        "@edge-runtime/vm": "^5.0.0",
        "convex-test": "^0.0.38",
        "typescript": "^5.9.2",
        "vitest": "^3.2.4"
    }
}
